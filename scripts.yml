scripts:
  start: deno run --allow-net --allow-env -c tsconfig.json src/main.ts
  start:dev: deno run --allow-net --allow-env --watch --unstable -c tsconfig.json src/main.ts
  test: deno test --allow-net src/
  format: deno fmt src/